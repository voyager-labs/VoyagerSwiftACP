{
  "skill_name": "codegraph-usage",
  "evals": [
    {
      "id": 1,
      "prompt": "ComposerSaveReducer.Action이 어디서 호출되는지 찾아줘.",
      "expected_output": "Selects codegraph_callers (not grep or rg), reports call sites with file:line locations for each caller.",
      "assertions": [
        "Uses codegraph_callers, not grep or rg",
        "Reports file:line locations for each call site",
        "Does not read all files in the Composer package"
      ]
    },
    {
      "id": 2,
      "prompt": "SearchGateway에서 search_router까지 호출 경로를 추적해줘.",
      "expected_output": "Uses codegraph_trace to find a multi-hop call path crossing Swift-to-Python boundary. If trace fails due to hop limit, explains fallback strategy using intermediate symbols.",
      "assertions": [
        "Uses codegraph_trace, not manual file-by-file reading",
        "Result shows a chain of hops from frontend to backend",
        "If trace fails, mentions picking an intermediate symbol as fallback"
      ]
    },
    {
      "id": 3,
      "prompt": "VoyagerFeaturesComposer 패키지 시그니처를 바꾸면 뭐가 깨지는지 영향 분석해줘.",
      "expected_output": "Uses codegraph_impact for initial scope analysis, then explicitly states that actual compilation and testing are still required to confirm breakage.",
      "assertions": [
        "Uses codegraph_impact for initial scope analysis",
        "Does NOT claim impact analysis replaces build/test verification",
        "Mentions running actual build or test commands to confirm"
      ]
    },
    {
      "id": 4,
      "prompt": "codegraph 도구를 쓰려는데 인덱스가 없거나 stale한 것 같아.",
      "expected_output": "Checks index status first via codegraph_status or just codegraph-status, runs init or reindex if needed, then proceeds with the query only after confirming index is ready.",
      "assertions": [
        "Runs codegraph_status or just codegraph-status first",
        "If index missing, runs just codegraph-init or just codegraph-reindex",
        "Does not proceed with queries before confirming index is ready"
      ]
    }
  ]
}
