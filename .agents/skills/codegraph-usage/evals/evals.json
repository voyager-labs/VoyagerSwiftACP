{
  "skill_name": "codegraph-usage",
  "evals": [
    {
      "id": 1,
      "prompt": "ComposerSaveReducer.Action이 어디서 호출되는지 찾아줘.",
      "expected_output": "Uses one codegraph_explore query asking for callers and returns file:line evidence without reopening returned files.",
      "assertions": [
        "Uses codegraph_explore",
        "Does not call removed CodeGraph tools",
        "Reports file:line evidence"
      ]
    },
    {
      "id": 2,
      "prompt": "SearchGateway에서 search_router까지 호출 경로를 추적해줘.",
      "expected_output": "Names both endpoints in one codegraph_explore query and reports the discovered call path.",
      "assertions": [
        "Uses codegraph_explore",
        "Includes both flow endpoints in the query",
        "Does not reconstruct the flow with grep and repeated reads"
      ]
    },
    {
      "id": 3,
      "prompt": "VoyagerFeaturesComposer 시그니처를 바꾸면 뭐가 깨지는지 분석해줘.",
      "expected_output": "Uses codegraph_explore for blast-radius analysis and requires compiler or test verification before claiming correctness.",
      "assertions": [
        "Uses codegraph_explore",
        "Requests impact or dependent context",
        "Does not claim CodeGraph replaces build or test verification"
      ]
    },
    {
      "id": 4,
      "prompt": "CodeGraph 인덱스가 없거나 stale한 것 같아.",
      "expected_output": "Calls codegraph_explore once, follows its banner or missing-index guidance, and does not invoke removed status/init commands.",
      "assertions": [
        "Uses codegraph_explore",
        "Does not call codegraph_status or local init tasks",
        "Handles the tool response before continuing"
      ]
    }
  ]
}
