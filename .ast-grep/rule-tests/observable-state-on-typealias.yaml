id: observable-state-on-typealias
valid:
  - |
    @ObservableState
    struct ExampleState { var count = 0 }
  - typealias State = ExampleState
invalid:
  - |
    @ObservableState
    typealias State = ExampleState
