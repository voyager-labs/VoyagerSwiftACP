# 1.1. SEARCH_DSL_SPEC

## 범위
- 조건 조합은 **AND만** 지원한다.
- 날짜/시간 값은 **절대 날짜**만 사용한다.
- 지원 propertyKey와 operator는 레지스트리(`shared/system_property_registry.json`)를 따른다.

## 요청 구조
```json
{
  "conditions": [
    {
      "propertyKey": "size",
      "operator": "gt",
      "value": 1048576
    }
  ],
  "scopes": ["/Users/me/Downloads"],
  "sort": "newest",
  "limit": 50,
  "offset": 0
}
```

### 필드
- `conditions`: 검색 조건 배열 (AND 결합)
- `scopes`: 폴더 범위 (절대 경로, 선택)
- `sort`: 정렬 옵션 (선택)
- `limit`: 페이지 크기 (선택)
- `offset`: 페이지 오프셋 (선택)

## Condition 스펙
- `propertyKey`: 레지스트리에 정의된 키만 허용
- `operator`: 레지스트리의 `supported_operators`에 포함된 값만 허용
- `value`: operator/value 타입 규칙에 맞는 값만 허용

### 지원 operator
- `eq` / `gt` / `gte` / `lt` / `lte`
- `between` (배열: `[min, max]`)
- `contains` (문자열)
- `in` (배열)

## 날짜/시간 규칙
- **절대 날짜만 허용**
- 포맷: `YYYY-MM-DD`
- 상대 날짜(예: last week, yesterday)는 **사전에 절대 날짜로 변환**되어야 한다.

## 정렬/페이지
- `sort`: `newest` | `oldest` | `largest` | `smallest`
- `limit`: 기본값/최대값 정책은 서버 구현을 따른다.
- `offset`: 0 이상의 정수

## 예시
```json
{
  "conditions": [
    {"propertyKey": "extension", "operator": "eq", "value": "pdf"},
    {"propertyKey": "modifiedAt", "operator": "between", "value": ["2025-01-01", "2025-01-31"]}
  ],
  "sort": "newest",
  "limit": 20
}
```

## 비지원 (명시)
- OR 조건
- 상대 날짜 표현
- 레지스트리에 없는 키/연산자
