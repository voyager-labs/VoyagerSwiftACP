import ComposableArchitecture
@testable import Voyager
@testable import VoyagerPagesFileManager
import XCTest

/// FileManager 윈도우 라우팅에서 명령이 적절한 채널로 위임되는지 검증한다.
@MainActor
final class FileManagerWindowCommandRoutingTests: XCTestCase {
    /// testVOY201WindowCommandRoutingPending 시나리오가 FileManager 계약을 위반하지 않음을 검증한다.
    func testVOY201WindowCommandRoutingPending() {}
}
