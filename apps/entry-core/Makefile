.PHONY: build test test-race smoke check

build:
	mise -C ../.. run entry-core-build

test:
	mise -C ../.. run entry-core-test

test-race:
	mise -C ../.. run entry-core-test-race

smoke:
	mise -C ../.. run entry-core-smoke

check:
	mise -C ../.. run entry-core-check
