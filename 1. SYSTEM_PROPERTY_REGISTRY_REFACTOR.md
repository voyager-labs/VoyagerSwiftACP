# 1. SYSTEM_PROPERTY_REGISTRY_REFACTOR

## 배경
- 시스템 프로퍼티 레지스트리가 Python 코드에 고정되어 있어 백엔드/Swift 간 중복 관리가 발생했다.
- 프롬프트/DSL/컴파일러에서 동일한 키/오퍼레이터 정보를 공유하기가 어려웠다.
- 배포 모드(source/bundled)에서 레지스트리 경로가 달라져 런타임 이슈가 생길 수 있었다.

## 목표
- **SSOT**: `shared/system_property_registry.json`을 단일 진실로 사용한다.
- 백엔드와 Swift 앱 번들에 **동일 파일**을 포함해 로딩한다.
- 레지스트리 변경 시 문서/DSL/컴파일러 불일치를 최소화한다.

## 데이터 포맷 선택 (JSON)
- Python/Swift 모두 **의존성 없이** 읽고 처리 가능하다.
- 단일 파일로 배포/번들링이 용이하다.
- JSON은 주석을 지원하지 않으므로, 문서로 보완한다.

## 레지스트리 구조 (키 기반)
- **배열 기반(`properties`)은 사용하지 않는다.**
- 최상위는 다음 두 개의 dict를 가진다.

```json
{
  "version": "1.0.0",
  "mditem_registry": {
    "kMDItemContentType": {
      "key": "kMDItemContentType",
      "db_field": "uniform_type_identifier",
      "type": "string",
      "description": "Uniform Type Identifier (UTI)",
      "examples": ["public.pdf"],
      "search_aliases": ["파일타입", "file type"],
      "category": "filesystem"
    }
  },
  "property_key_registry": {
    "size": {
      "property_key": "size",
      "db_field": "size",
      "json_path": null,
      "value_type": "number",
      "supported_operators": ["eq", "gt", "gte", "lt", "lte", "between"]
    }
  }
}
```

### 필수 필드
- `mditem_registry`
  - `key`, `type`, `description`, `search_aliases`, `category`
- `property_key_registry`
  - `property_key`, `value_type`, `supported_operators`

### 선택 필드
- `mditem_registry`: `db_field`, `examples`
- `property_key_registry`: `db_field`, `json_path`

## 배포 경로 정책
- **source 모드(로컬)**
  - repo root의 `shared/system_property_registry.json` 사용
- **bundled 모드(백엔드 바이너리)**
  - Nuitka 빌드 산출물에 `shared/system_property_registry.json` 포함
  - 실행 바이너리 기준 상위 경로에서 `shared/` 탐색
- **Swift 앱**
  - 앱 번들 리소스로 포함
  - `Bundle.main.url(forResource: "system_property_registry", withExtension: "json")`로 로딩

## shared로 이동하는 이유
- 백엔드/Swift가 **동일 파일**을 참조하여 중복 정의를 제거한다.
- 레지스트리 변경 시 **한 번의 수정**으로 전 파이프라인 동기화가 가능하다.
- 번들/배포 파이프라인에서 단일 리소스로 관리하기 용이하다.

## 버전 필드 사용 이유
- 레지스트리 구조 변경 시 **호환성 판단** 기준 제공
- 캐시/클라이언트 로딩 시 **무효화 조건**으로 활용 가능
- 릴리즈 노트/문서 업데이트 시 **추적 가능한 식별자**로 사용

## 키별 operator/value 타입 갱신 규칙
- 레지스트리의 각 키는 지원 operator 목록과 value 타입을 **반드시 명시**해야 한다.
- operator를 추가/변경할 때는 해당 키의 value 타입과 **호환성**을 먼저 검토한다.
  - 예: `between`은 숫자/날짜 타입만 허용
  - 예: `contains`는 문자열 타입만 허용
- operator 추가 시 Search DSL 문서와 컴파일러 규칙도 **동시 갱신**한다.
- 문서/레지스트리/컴파일러 간 **불일치 방지**를 최우선으로 한다.

### 현실적으로 1차 반영 가능한 operator
- 문자열/파일명
  - `startsWith`, `endsWith`
- 숫자/범위
  - `notEq`, `notIn`
- 불리언/존재 여부
  - `isNull`, `isNotNull`
- 정렬/페이징(DSL 옵션)
  - `sort`, `limit`, `offset`

## 예상 사이드이펙트 (모두 고려)
- **백엔드 로딩 경로 변경**
  - 기존 Python 상수 → JSON 로딩 전환
  - `REGISTRY_PATH` 환경변수 미설정 시 경로 탐색 실패 가능
- **bundled/backend 경로 차이**
  - Nuitka dist에 JSON 누락 시 런타임 에러 발생
- **Swift 번들 리소스 누락 위험**
  - 리소스 미포함 시 로딩 실패 및 UI 기능 제한 가능
- **문서/코드 불일치 위험**
  - 레지스트리/DSL/컴파일러 갱신 순서가 어긋나면 오류 발생
- **프로젝트 구조 변경 영향**
  - `shared/` 경로 이동 시 모든 로더가 함께 수정되어야 함

## 변경 범위
- `shared/system_property_registry.json` 추가
- 백엔드 JSON 로더 전환 및 빌드 시 포함
- Swift 앱 번들 리소스 포함 및 런타임 로더 추가

## 롤백
- JSON 로더 제거 → 기존 Python 레지스트리 복구
- Nuitka 빌드 포함 옵션 제거
- Swift 번들 리소스/로더 제거

## 연동 문서
- `1.1. SEARCH_DSL_SPEC.md`
