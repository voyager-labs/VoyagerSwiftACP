# Story 117.2: Helper 종료 감지 및 재시작 정책

> Superseded: 이 스토리는 폐기되었습니다. 새 스토리: `docs/voy-117/stories/117.10.helper-restart-watchdog.md`

## Status
Superseded

## Story
**As a** 사용자,  
**I want** Helper가 비정상 종료되더라도 자동으로 복구되길,  
**so that** Voyager 사용 중 기능이 안정적으로 유지된다.

## Acceptance Criteria
1. Voyager 실행 중 Helper 종료 이벤트를 감지하면 backoff 정책에 따라 재실행한다.
2. backoff는 1s → 2s → 4s → 8s → 16s(최대 5회 재시도)로 적용한다.
3. 재시도 중에는 사용자에게 UI 알림을 노출하지 않고 로그만 남긴다.
4. 최대 재시도 초과 시 Voyager는 즉시 종료(Crash)된다.
5. Voyager 종료 시 Helper를 강제로 종료하지 않는다.
6. Helper 재실행은 중복 실행을 방지한다.
7. 기존 앱 종료 흐름과 충돌하지 않는다.
8. 재시도/종료 이벤트가 로그로 확인 가능하다.
9. 변경 전후 회귀 동작이 수동 QA로 확인된다.
10. 최대 재시도 초과 시 `exit(EXIT_FAILURE)`로 즉시 종료한다.
11. Helper가 2초 이상 정상 실행되면 재시도 카운트가 초기화된다.
12. 앱 종료 시 모니터링 루프는 중단되어 재시작이 발생하지 않는다.
13. 재시도 로그는 종료 원인/exit code/재시도 카운트를 포함한다.

## Tasks / Subtasks
- [ ] Helper 종료 이벤트 감지 루프에 backoff/재시도 상태 추가 (AC: 1, 2)
  - [ ] 재시도 카운트 및 최대 재시도 조건 정의
- [ ] 재시도 중 로그 처리만 유지 (AC: 3, 8)
- [ ] 최대 재시도 초과 시 즉시 종료 정책 반영 (AC: 4)
- [ ] 앱 종료 시 Helper 종료 로직 제거/무시 확인 (AC: 5, 7)
- [ ] 중복 실행 방지 확인 (AC: 6)
- [ ] 종료 방식은 `exit(EXIT_FAILURE)`로 통일 (AC: 10)
- [ ] 정상 기동 판정 후 재시도 카운트 리셋 (AC: 11)
- [ ] 앱 종료 시 모니터링 루프 중단 처리 (AC: 12)
- [ ] 종료 원인/exit code/재시도 카운트 로그 포함 (AC: 13)
- [ ] 수동 QA 수행 (AC: 9)

## Dev Notes
- 보류: Story 117.9 완료 후 재개한다. 엔드포인트 전달/async 전환 확정 후 재시도 정책 QA를 진행한다.
- `AppLifecycleFeature`가 `helperAppClient.terminationEvents()`를 감시한다.
- `HelperAppClient`는 `NSWorkspace.didTerminateApplicationNotification` 기반으로 종료 이벤트를 제공한다.
- 재시도 정책은 `AppLifecycleFeature`의 감시 루프 내부에 상태로 유지하는 편이 단순하다.
- 즉시 종료(Crash) 정책은 앱 종료 방식(예: `NSApp.terminate(nil)` 또는 `exit`)을 명시해야 한다.
- 즉시 종료는 `exit(EXIT_FAILURE)`로 통일한다.
- 정상 기동 판정은 "Helper가 2초 이상 실행 상태 유지"로 정의하고, 성공 시 재시도 카운트를 리셋한다.
- 앱 종료 시 모니터링 루프를 중단해 재시작을 억제한다.
- 로그에는 종료 원인/exit code/재시도 카운트를 포함한다.
- 참고: `docs/voy-117/epic.md#epic-goal`
- 참고: `docs/architecture/source-tree.md#핵심-모듈과-역할`

### Testing
- 수동: Helper 강제 종료 시 backoff 재시작이 5회까지 진행되는지 확인.
- 수동: 최대 재시도 초과 시 Voyager가 즉시 종료되는지 확인.
- 수동: Voyager 종료 시 Helper가 종료되지 않는지 확인.
- 수동: Helper가 2초 이상 실행된 뒤 재시도 카운트가 리셋되는지 확인.
- 수동: 앱 종료 중 재시작이 발생하지 않는지 확인.
- 수동: 종료 원인/exit code/재시도 카운트 로그가 기록되는지 확인.

## Change Log
| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-01-07 | v0.1 | 스토리 템플릿 정리 및 Dev Notes 추가 | Sarah (PO) |
| 2026-01-07 | v0.2 | YOLO 리뷰 반영 | Bob (SM) |
| 2026-01-07 | v0.3 | 우선순위 변경으로 보류 처리 | Sarah (PO) |
| 2026-01-07 | v0.4 | 재시도/종료 기준 및 로그 보강 | Bob (SM) |
| 2026-01-08 | v0.5 | 상태 Review로 갱신 | Bob (SM) |
| 2026-01-08 | v0.5 | Story 117.8 전환에 따른 보류 기준 갱신 | Sarah (PO) |
| 2026-01-08 | v0.6 | Story 117.9 선행 반영 | Sarah (PO) |

## Dev Agent Record
### Agent Model Used
TBD

### Debug Log References
TBD

### Completion Notes List
- TBD

### File List
- TBD

## QA Results
TBD
