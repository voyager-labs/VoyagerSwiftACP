# Story 117.4: UI 엔드포인트 탐지 정리

## Status
Superseded (117.8으로 전환)

## Story

**As a** 사용자,
**I want** UI가 backend 엔드포인트를 자동 탐지하길,
**so that** 동적 포트에서도 안정적으로 요청을 보낼 수 있다.

## Acceptance Criteria

1. UI는 `pgrep -f "Voyager Backend"` + `lsof -n -P -a -iTCP -sTCP:LISTEN -p <PID>`로 endpoint를 구성한다.
2. `PUBLIC_BACKEND_PORT=0` 환경에서도 endpoint 탐지가 정상 동작한다.
3. backend 재기동 시 UI는 endpoint를 재탐지한다.
4. runtime config/BackendState 공유 로직은 제거된다.
5. PID는 프로세스명으로부터 확보하고, lsof로 LISTEN 포트를 추출해 URL을 구성한다.
6. 다중 PID 매칭 시 `ps`의 `etime` 기준으로 시작 시간이 가장 최근인 프로세스를 우선 선택한다.
7. URL은 `PUBLIC_BACKEND_HOST` + 탐지한 포트로 구성한다.
8. source/bundled 모두 `Voyager Backend` 프로세스명 탐지가 가능하다.
9. 기존 UI 흐름과 충돌하지 않는다.
10. 탐지 실패 시 1초 간격으로 최대 10회 자동 재시도한다.
11. 10회 실패 후에는 로그만 남기고 `nil`을 반환한다.
12. 실패 후에도 UI는 프리즈되지 않고 브라우징이 가능해야 한다.
13. 변경 전후 회귀 동작이 수동 QA로 확인된다.
14. `PUBLIC_BACKEND_PROCESS_NAME`는 `.env`에서만 읽고 런처 env는 사용하지 않는다.

## Tasks / Subtasks

- [X] PID 탐지 유틸 구현 (`pgrep -f "Voyager Backend"`) (AC: 1, 5)
- [X] 포트 탐지 유틸 구현 (`lsof ... LISTEN`) (AC: 1, 5)
- [X] 다중 PID 선택 정책 구현 (시작 시간 기준) (AC: 6)
- [X] URL 구성 로직 구현 (`PUBLIC_BACKEND_HOST` + 포트) (AC: 7)
- [X] runtime config/BackendState 공유 로직 존재 여부 확인 후 제거 또는 N/A 기록 (AC: 4)
- [X] 실패 시 1초 간격 최대 10회 재시도 후 로그만 남기고 `nil` 반환 (AC: 10~12)
- [X] `pgrep`/`lsof` 실행은 백그라운드에서 수행하도록 구현 (AC: 9)
- [X] endpoint 탐지 로직의 통합 지점 확정 및 연결 (AC: 9)
- [X] `.env`에서 `PUBLIC_BACKEND_PROCESS_NAME`을 로드하도록 구현 (AC: 14)
- [ ] 수동 QA 수행 (AC: 2, 8, 14)
- [ ] `BackendEndpointClient` 환경 로딩/검증 단계 분리 및 로깅 기준 정리 (Architect Plan)
- [ ] `BackendEndpointClient` PID 선택/포트 탐지 단계 재구성 (Architect Plan)
- [ ] `BackendEndpointClient` 재시도/실패 로그 메시지 표준화 (Architect Plan)

## Dev Notes

- 우선순위: Story 117.4를 선행한다. endpoint 탐지 확정 전에는 재기동/헬퍼 정책 QA가 막히므로 먼저 정리한다.
- Story 117.3의 Helper 핸드셰이크 제거는 후속으로 진행하며, 본 스토리는 UI 탐지 로직에 집중한다.
- UI 클라이언트는 `apps/macos/Voyager/Voyager/App/Clients` 아래에 신규 추가를 고려한다.
- 프로세스명은 source/bundled 모두 `Voyager Backend`를 사용한다.
- 프로세스명 SSOT: `.env`의 `PUBLIC_BACKEND_PROCESS_NAME`만 사용한다. 런처 env는 `APP_ENV`/`BACKEND_MODE`/`VOYAGER_PROJECT_ROOT`만 허용한다.
- UI 앱에서도 `SwiftDotenv`로 `.env.{APP_ENV}`를 로드해 `PUBLIC_BACKEND_PROCESS_NAME`을 읽는다.
- endpoint 구성은 `PUBLIC_BACKEND_HOST`를 기준으로 한다.
- `PUBLIC_BACKEND_PROCESS_NAME`/`PUBLIC_BACKEND_HOST` 이외의 env는 탐지 로직에서 읽지 않는다.
- 다중 PID는 `ps -o pid= -o etime=` 결과를 사용해 경과 시간이 가장 짧은 PID를 선택한다.
  - `etime` 포맷(`[[dd-]hh:]mm:ss`)을 초 단위로 파싱해 비교한다.
  - 오버헤드: `ps` 1회 호출 + 문자열 파싱(수 ms 수준 예상). PID 수가 적은 환경에서는 체감 영향 없음.
- Helper의 stderr 포트 파싱/핸드셰이크는 제거하고 UI가 직접 탐지한다. (117.3 범위)
- 의존성: `docs/voy-117/stories/117.3.backend-restart-management.md`의 핸드셰이크 제거 방향과 일치해야 한다.
- `pgrep`/`lsof` 실행은 UI 프리즈 방지를 위해 백그라운드에서 수행한다.
- `pgrep` 결과가 없거나 `lsof`에서 LISTEN 포트가 없으면 실패로 간주하고 재시도 카운트를 증가한다.
- `PUBLIC_BACKEND_HOST`가 없으면 탐지 실패로 처리하고 로그만 남긴다.
- runtime config/BackendState 관련 코드는 현재 코드베이스에서 발견되지 않아, 존재 여부 확인 후 결과를 문서화한다.
- 통합 지점은 신규 `BackendEndpointClient`(예: `apps/macos/Voyager/Voyager/App/Clients/BackendEndpointClient.swift`)로 캡슐화하고, 실제 API 호출 지점에 의존성으로 주입한다.
- 참고: `docs/voy-117/epic.md#epic-goal`
- 참고: `docs/architecture/tech-stack.md#기술-요약`
- 참고: `docs/architecture/source-tree.md#핵심-모듈과-역할`

## Architect Plan

- 목표: `BackendEndpointClient`의 엔드포인트 탐지 흐름을 단순화하고 책임을 명확히 분리한다.
- 범위:
  - `apps/macos/Voyager/Voyager/App/Clients/BackendEndpointClient.swift`
- 단계:
  1. env 로딩/검증을 별도 단계로 분리하고, 실패 시 로그 기준을 통일한다.
  2. PID 탐지 → 최신 PID 선택 → 포트 탐지 흐름을 작은 단계로 분리해 가독성을 개선한다.
  3. 재시도/실패 로그 메시지를 표준화하고, 실패 원인 분류 기준을 문서화한다.

### Testing

- 수동: backend 실행 중 `PUBLIC_BACKEND_PORT=0`에서 endpoint 탐지가 되는지 확인.
- 수동: backend 재기동 후 UI가 endpoint를 재탐지하는지 확인.
- 수동: 탐지 실패 시 10회 재시도 후 로그만 남기고 UI가 계속 동작하는지 확인.
- 수동: 다중 PID 매칭 시 시작 시간이 가장 최근인 프로세스가 선택되는지 확인.

## Change Log

| Date       | Version | Description                                              | Author      |
| ---------- | ------- | -------------------------------------------------------- | ----------- |
| 2026-01-07 | v0.1    | 스토리 템플릿 정리 및 Dev Notes 추가                     | Sarah (PO)  |
| 2026-01-07 | v0.2    | YOLO 리뷰 반영                                           | Bob (SM)    |
| 2026-01-07 | v0.3    | 다중 PID 선택 정책 확정                                  | Sally (UX)  |
| 2026-01-07 | v0.4    | Helper 핸드셰이크 제거 방향 명시                         | Sarah (PO)  |
| 2026-01-07 | v0.5    | 실패/실행 컨텍스트 보강                                  | Sarah (PO)  |
| 2026-01-07 | v0.6    | 통합 지점/제거 범위 명시                                 | Sarah (PO)  |
| 2026-01-07 | v0.7    | PUBLIC_* SSOT 기준 반영                                  | Sarah (PO)  |
| 2026-01-07 | v0.8    | 우선순위 변경 사유 반영                                  | Sarah (PO)  |
| 2026-01-07 | v0.9    | 스토리 리뷰 및 의존성 표현 정리                          | Bob (SM)    |
| 2026-01-07 | v1.0    | UI 엔드포인트 탐지 구현                                  | James (Dev) |
| 2026-01-07 | v1.1    | NSAlert 제거 및 PID 선택 기준 변경                       | Sarah (PO)  |
| 2026-01-07 | v1.2    | PID 선택 기준을 `ps etime`으로 구체화                  | Sarah (PO)  |
| 2026-01-07 | v1.3    | `ps etime` 기반 PID 선택 및 재시도 실패 로그 처리 반영 | James (Dev) |
| 2026-01-08 | v1.4    | 엔드포인트 탐지 재시도 횟수 상향                       | James (Dev) |
| 2026-01-08 | v1.5    | Architect Plan 추가 및 문서 정합성 정리                | Codex (Architect) |
| 2026-01-08 | v1.6    | Architect Plan 범위/Task를 BackendEndpointClient로 정리 | Codex (Architect) |
| 2026-01-08 | v1.7    | 옵션 B 스토리로 전환 표기                                | Sarah (PO)  |

## Dev Agent Record

### Agent Model Used

GPT-5 (Codex)

### Debug Log References

없음

### Completion Notes List

- `BackendEndpointClient`에 `.env` 기반 프로세스명/호스트 로드 + `pgrep`/`lsof` 탐지 후 `ps etime` 기준으로 최신 PID 선택을 추가.
- endpoint 탐지는 1초 간격 최대 10회 재시도 후 실패 시 로그만 남기고 `nil` 반환하도록 변경(NSAlert 제거).
- `AppLifecycleFeature`에서 런치 및 Helper 종료 이벤트마다 endpoint 탐지를 호출.
- runtime config/BackendState 공유 로직은 코드베이스에서 미발견(N/A).
- `xcodebuild -project apps/macos/Voyager/Voyager.xcodeproj -scheme Voyager-Dev -configuration Debug build` 성공.
- 수동 QA 미수행.

### File List

- apps/macos/Voyager/Voyager/App/Clients/BackendEndpointClient.swift
- apps/macos/Voyager/Voyager/App/Features/AppLifecycleFeature.swift

## QA Results

### Review Date: 2026-01-07

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

구현은 AC 요구에 맞는 흐름으로 구성되어 있으며, 비동기 처리/로깅 위치도 일관적입니다. 다만 수동 QA가 미수행이라 동작 검증은 완료되지 않았습니다. 자동화 테스트는 UI 테스트를 제외하고 통과했습니다.

### Refactoring Performed

없음.

### Compliance Check

- Coding Standards: ✓ (Swift 스타일/구조 준수)
- Project Structure: ✓ (지정 경로 및 구조 준수)
- Testing Strategy: ✗ (전용 문서 미확인 + 수동 QA 미수행)
- All ACs Met: ✗ (구현은 확인되나 수동 QA로 검증 미완료)

### Improvements Checklist

- [ ] 수동 QA 실행 및 결과 기록 (AC: 2, 3, 8, 10~12, 14)
- [ ] 실패 사유를 구분할 수 있는 로그(예: pgrep/lsof 실패 원인) 추가 여부 검토
- [ ] 프로세스 탐지/파싱 로직에 대한 단위 테스트 추가 검토

### Security Review

새로운 민감 정보 처리나 권한 상승 로직이 없으며, 보안 이슈는 발견되지 않았습니다.

### Performance Considerations

`ps` 호출은 재시도 최대 10회 범위에서만 발생하며, PID 수가 적은 환경에서는 체감 오버헤드가 낮습니다.

### Files Modified During Review

없음.

### Gate Status

Gate: CONCERNS → docs/qa/gates/117.4-ui-endpoint-discovery.yml
Risk profile: 미작성 (요청 시 생성 가능)
NFR assessment: 미작성 (요청 시 생성 가능)

### Recommended Status

[✗ Changes Required - See unchecked items above]
(Story owner decides final status)

### Manual QA 재실행 (2026-01-07)

- 환경: Debug 앱 직접 실행 + Helper 수동 실행, `VOYAGER_PROJECT_ROOT`/`BACKEND_MODE=source`
- 결과: FAIL ( `PUBLIC_BACKEND_PROCESS_NAME` 누락으로 endpoint 탐지 실패 )
- 로그: `/tmp/voyager-qa-debug.err` ( `Backend: resolving` → `Missing PUBLIC_BACKEND_PROCESS_NAME in .env` ×3 → `Backend endpoint discovery failed` → `Backend: failed` )
- 커버리지: AC 10~12 실패/재시도 동작 확인. AC 2/8/14는 `.env` 키 추가 후 재검증 필요.

### Manual QA 재실행 (2026-01-08)

- 환경: Debug 앱 바이너리 직접 실행 (`APP_ENV=dev`, `BACKEND_MODE=source`, `VOYAGER_PROJECT_ROOT` 설정)
- 결과: FAIL (약 2초 내 3회 재시도 후 실패; backend 준비 이전에 탐지 종료)
- 관찰: `pgrep -fl "Voyager Backend"` 결과 없음, `uv run dev`만 확인됨 (프로세스명 불일치)
- 로그: `/tmp/voyager-qa-1174-20260108.log` ( `Backend: resolving` → `Backend endpoint discovery failed after 3 attempts` → `Backend: failed` )
