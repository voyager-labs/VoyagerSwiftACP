# Story 117.4: UI 엔드포인트 탐지 정리

## Status
Draft

## Story
**As a** 사용자,  
**I want** UI가 backend 엔드포인트를 자동 탐지하길,  
**so that** 동적 포트에서도 안정적으로 요청을 보낼 수 있다.

## Acceptance Criteria
1. UI는 `pgrep -f "Voyager Backend"` + `lsof -n -P -a -iTCP -sTCP:LISTEN -p <PID>`로 endpoint를 구성한다.
2. `PUBLIC_BACKEND_PORT=0` 환경에서도 endpoint 탐지가 정상 동작한다.
3. backend 재기동 시 UI는 endpoint를 재탐지한다.
4. runtime config/BackendState 공유 로직은 제거된다.
5. PID는 프로세스명으로부터 확보하고, lsof로 LISTEN 포트를 추출해 URL을 구성한다.
6. 다중 PID 매칭 시 최신 PID(가장 큰 PID)를 우선 선택한다.
7. URL은 `PUBLIC_BACKEND_HOST` + 탐지한 포트로 구성한다.
8. source/bundled 모두 `Voyager Backend` 프로세스명 탐지가 가능하다.
9. 기존 UI 흐름과 충돌하지 않는다.
10. 탐지 실패 시 1초 간격으로 3회 자동 재시도 후 NSAlert를 표시한다.
11. NSAlert 메시지: "Cannot connect to Voyager Backend"
12. NSAlert 설명: "Voyager couldn't find a running backend. Please wait a few seconds and retry."
13. NSAlert 버튼: "Retry"(기본), "Quit"(보조). Retry 시 탐지를 즉시 재시도한다.
14. 변경 전후 회귀 동작이 수동 QA로 확인된다.

## Tasks / Subtasks
- [ ] PID 탐지 유틸 구현 (`pgrep -f "Voyager Backend"`) (AC: 1, 5)
- [ ] 포트 탐지 유틸 구현 (`lsof ... LISTEN`) (AC: 1, 5)
- [ ] 다중 PID 선택 정책 구현 (최신 PID 우선) (AC: 6)
- [ ] URL 구성 로직 구현 (`PUBLIC_BACKEND_HOST` + 포트) (AC: 7)
- [ ] runtime config/BackendState 공유 로직 제거 (AC: 4)
- [ ] 실패 시 1초 간격 3회 재시도 후 NSAlert 표시 (AC: 10~13)
- [ ] 수동 QA 수행 (AC: 2, 8, 14)

## Dev Notes
- UI 클라이언트는 `apps/macos/Voyager/Voyager/App/Clients` 아래에 신규 추가를 고려한다.
- 프로세스명은 source/bundled 모두 `Voyager Backend`를 사용한다.
- endpoint 구성은 `PUBLIC_BACKEND_HOST`를 기준으로 한다.
- `pgrep` 결과는 PID 내림차순으로 정렬한 뒤 LISTEN 포트가 있는 첫 PID를 선택한다.
- Helper의 stderr 포트 파싱/핸드셰이크는 제거하고 UI가 직접 탐지한다. (117.3 범위)
- 참고: `docs/voy-117/epic.md#epic-goal`
- 참고: `docs/architecture/tech-stack.md#기술-요약`
- 참고: `docs/architecture/source-tree.md#핵심-모듈과-역할`

### Testing
- 수동: backend 실행 중 `PUBLIC_BACKEND_PORT=0`에서 endpoint 탐지가 되는지 확인.
- 수동: backend 재기동 후 UI가 endpoint를 재탐지하는지 확인.
- 수동: 탐지 실패 시 3회 재시도 후 NSAlert가 표시되는지 확인.
- 수동: 다중 PID 매칭 시 최신 PID가 선택되는지 확인.

## Change Log
| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-01-07 | v0.1 | 스토리 템플릿 정리 및 Dev Notes 추가 | Sarah (PO) |
| 2026-01-07 | v0.2 | YOLO 리뷰 반영 | Bob (SM) |
| 2026-01-07 | v0.3 | 다중 PID 선택 정책 확정 | Sally (UX) |
| 2026-01-07 | v0.4 | Helper 핸드셰이크 제거 방향 명시 | Sarah (PO) |

## Dev Agent Record
### Agent Model Used
TBD

### Debug Log References
TBD

### Completion Notes List
- TBD

### File List
- TBD

## QA Results
TBD
