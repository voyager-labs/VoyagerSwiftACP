{
  "schema_version": "1.0",
  "name": "OBJECTS",
  "primary_key": ["key"],
  "columns": [
    {
      "name": "category",
      "type": "string",
      "required": true,
      "enum": ["Domain", "Conversation", "State", "Operation", "Rule"],
      "description": "오브젝트 상위 분류(그룹).",
      "enum_descriptions": {
        "Domain": "제품 전반의 기본 개체.",
        "Conversation": "대화, 세션, 요청, 응답 계열 개체.",
        "State": "UI나 실행 중 유지되는 상태성 개체.",
        "Operation": "실행 가능한 동작 단위.",
        "Rule": "판정 또는 적용 규칙."
      }
    },
    {
      "name": "key",
      "type": "string",
      "required": true,
      "description": "오브젝트 식별자(PK). 안정적인 참조 키."
    },
    {
      "name": "label_ko",
      "type": "string",
      "required": false,
      "description": "한글 라벨."
    },
    {
      "name": "summary",
      "type": "string",
      "required": false,
      "description": "오브젝트 한 줄 정의(무엇인지)."
    }
  ]
}
