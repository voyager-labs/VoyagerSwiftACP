{
  "schema_version": "1.0",
  "name": "FEATURES",
  "primary_key": ["feature_id"],
  "columns": [
    {
      "name": "feature_category",
      "type": "string",
      "required": true,
      "ref": {
        "table": "FEATURE_CATEGORIES",
        "column": "category_title"
      },
      "description": "카테고리 제목(표시용, FEATURE_CATEGORIES.category_title)."
    },
    {
      "name": "category_key",
      "type": "string",
      "required": true,
      "ref": {
        "table": "FEATURE_CATEGORIES",
        "column": "category_key"
      },
      "description": "기능 카테고리 키(FK -> FEATURE_CATEGORIES.category_key)."
    },
    {
      "name": "feature_title",
      "type": "string",
      "required": true,
      "description": "기능 제목(사람이 읽는 라벨)."
    },
    {
      "name": "feature_id",
      "type": "string",
      "required": true,
      "description": "기능 ID(PK). 안정적인 참조 키."
    },
    {
      "name": "release_phase",
      "type": "string",
      "required": false,
      "enum": [
        "BACKLOG",
        "CBT-W1",
        "CBT-NEXT",
        "OBT",
        "MVP",
        "LATER"
      ],
      "description": "릴리즈 단계 태그(예: BACKLOG, CBT-W1, CBT-NEXT, OBT, MVP, LATER)."
    },
    {
      "name": "description",
      "type": "string",
      "required": false,
      "description": "기능 한 줄 설명(무엇을 하는지)."
    },
    {
      "name": "related_ui",
      "type": "string",
      "required": false,
      "ref": {
        "table": "WINDOW_STRUCTURE",
        "column": "structure_key"
      },
      "description": "UI 구조 참조(옵션, FK -> WINDOW_STRUCTURE.structure_key). 기능과 가장 연관이 높은 대표 UI 영역."
    }
  ]
}
