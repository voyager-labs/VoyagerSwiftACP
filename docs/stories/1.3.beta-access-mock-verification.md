# Story 1.3: Beta Access Step (Mock Verification)

## Status

Done

## Story

**As a** 베타 사용자,  
**I want** `Beta Access` 단계에서 이메일/토큰을 입력하고 모킹된 검증 결과를 확인하며,  
**so that** 실제 서버 연동 전에도 온보딩 게이팅/UX를 검증할 수 있다.

## Acceptance Criteria

1. `Beta Access` 스텝은 이메일/토큰 입력 필드를 제공하고 입력값으로 검증을 시도한다.
2. 검증 결과는 `Active` / `Not Active` / `Check failed` 상태로 표시된다.
3. `Not Active` 상태는 reason code로 구분되며, 사유별 가이드를 제공한다: `missingInput`, `mismatch`, `tokenAlreadyRegistered`.
4. 하드코딩된 시나리오 순환은 `BetaAccessFeature.State`의 `scenarioIndex`로 관리하며, 이메일/토큰이 모두 입력된 검증 시도에만 인덱스를 1씩 증가(모듈러 4)한다. 순환 순서는 `Mismatch` → `Token already registered` → `Check failed` → `Active` (반복).
5. `Active`가 되기 전까지 `Next`는 비활성화되고, `Active`가 되면 `Next`가 활성화된다.
6. `Check failed` 상태는 일시적 검증 실패로 간주하고 `Retry`로 재시도할 수 있으며, 재시도 시 다음 순환 결과로 진행된다(원인/다음 행동 가이드 제공).

## Tasks / Subtasks

- [x] Define mock verification spec + scenario rotation (AC: 1–4)
  - [x] Define input validation rules (email/token required)
  - [x] Define status model (`status` + `reason`) and `scenarioIndex` storage
  - [x] Define hardcoded scenario rotation order + status mapping
  - [x] Define Not Active reason codes for UX copy
- [x] Implement `BetaAccessFeature` (TCA) + dependency injection (AC: 1–5)
  - [x] Add a `BetaAccessClient` TCA dependency (mock live + test)
  - [x] Add `emailChanged` / `tokenChanged` actions
  - [x] Store `scenarioIndex` in state and advance only on valid inputs
  - [x] Implement `BetaAccessFeature` reducer handling:
    - [x] `onAppear` → trigger verification (validate inputs + scenario rotation)
    - [x] `retryTapped` → trigger verification (cancel in-flight if needed)
    - [x] `verificationResponse` → set status + reason
- [x] Implement Beta Access step UI (SwiftUI + TCA) (AC: 1–4)
  - [x] Render email/token inputs + status view
  - [x] Provide guidance copy for Not Active reasons + Check failed (see UX Decisions)
  - [x] Ensure `Next` remains disabled until `Active` (gating owned by `OnboardingFeature`)
- [x] Integrate with Onboarding root gating (AC: 5)
  - [x] `OnboardingFeature` uses `BetaAccessFeature.State` to decide `Next` availability
- [x] Align module layout with current structure
  - [x] Move `BetaAccess` modules to `App/{Client,Features,Models}`
  - [x] Split step reducers into `Onboarding/Features/{Welcome,Permissions,IndexingPreset,Complete}`
- [x] Tests (AC: 1–5)
  - [x] Unit tests for scenario rotation + validation using `TestStore`
  - [x] Coverage for mismatch / token already registered / check failed / active
  - [x] Coverage for missing input (no rotation advance)

## Dev Notes

- Depends on: Story 1.2 (step state machine exists)
- UX/AC source:
  - `docs/frontend/onboarding-window-spec.md#beta-access`
  - `docs/frontend/onboarding-window-spec.md#acceptance-criteria-locked-for-voy-111`
- Reference summary:
  - Beta Access는 `Active`/`Not Active`/`Check failed` 상태 표시와 `Retry` CTA를 가진다.
  - `Active` 전까지 `Next`는 비활성화되고, 실패 시 재시도/가이드만 제공한다(우회 없음).
- TCA architecture notes:
  - This story should be implemented as the `BetaAccessFeature` child reducer scoped from the root `OnboardingFeature` (created in Story 1.2).
  - `OnboardingFeature` owns cross-step rules (`Next` gating, progress header). `BetaAccessFeature` owns verification mechanics and its local status UI.
  - Module layout (current):
    - `apps/macos/Voyager/Voyager/App/{Client,Features,Models}` for Beta Access domain
    - `apps/macos/Voyager/Voyager/Onboarding/Features` for step reducers + `OnboardingFeature`
    - `apps/macos/Voyager/Voyager/Onboarding/Views` for step UI
- This story is **mock-only**. Real server verification is split into Story 1.6.
- UX guidance copy for failure reasons will be provided by UX Expert.
- Mock model guidance:
  - `status`: `active` | `notActive` | `checkFailed`
  - `reason`: `missingInput` | `mismatch` | `tokenAlreadyRegistered` | `none`
  - `scenarioIndex`: 0~3 순환, 유효 입력 시에만 증가

### UX Decisions

- 입력 필드 라벨: `Email`(이메일), `Token`(토큰)
- 플레이스홀더: `email@example.com`(이메일 예시), `Paste your token here`(토큰을 여기에 붙여넣기)
- 도움말 문구: “Enter the email and token from your invitation email.”(초대 메일에 있는 이메일과 토큰을 입력해 주세요.)
- 토큰 입력은 기본 마스킹(`SecureField`)으로 처리하고 붙여넣기 사용을 전제로 한다.
- 검증 CTA는 상태에 따라 라벨을 전환한다: 기본은 `Check`(확인), `Check failed` 상태에서는 `Retry`(재시도).
- CTA는 이메일/토큰이 모두 입력될 때만 활성화한다.
- `onAppear`에서 입력 유효성만 먼저 검사하고, 미입력일 경우 `Not Active` + `missingInput` 가이드를 노출한다.
- 상태 라벨은 영문(`Active` / `Not Active` / `Check failed`)으로 노출하며, 문서에는 한국어 번역을 병기한다.
- 검증 중에는 스피너를 표시하고 CTA/입력을 비활성화한다.

### UX Copy (Draft)

- `Active`
  - 메시지: “Your beta access is confirmed. You can continue to the next step.”(베타 접근이 확인되었습니다. 다음 단계로 진행할 수 있어요.)
- `Not Active` + `missingInput`
  - 메시지: “Please enter both your email and token. Check your invitation email for the details.”(이메일과 토큰을 모두 입력해 주세요. 초대 메일에서 정보를 확인해 주세요.)
- `Not Active` + `mismatch`
  - 메시지: “That email and token don’t match. Please double-check the invitation email.”(이메일과 토큰이 일치하지 않아요. 초대 메일 정보를 다시 확인해 주세요.)
- `Not Active` + `tokenAlreadyRegistered`
  - 메시지: “Each token can be registered to one device only. If you want to switch devices, please contact the invite owner.”(토큰은 하나의 디바이스에만 등록할 수 있어요. 기기를 변경하려면 초대 담당자에게 연락해 주세요.)
- `Check failed`
  - 메시지: “We couldn’t verify right now. Check your connection and try again.”(지금 확인할 수 없어요. 네트워크 상태를 확인한 뒤 다시 시도해 주세요.)

### Testing

- Unit tests should simulate:
  - Missing email/token → Not Active + guidance
  - Mismatch → Not Active + guidance
  - Token already registered → Not Active + guidance
  - Check failed + Retry loop
  - Active → Next enabled

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-01-03 | v0.1 | Initial draft for VOY-111 Story 1.3 | scrum-master |
| 2026-01-03 | v0.2 | Add TCA architecture guidance (BetaAccessFeature + dependencies + gating) | scrum-master |
| 2026-01-04 | v0.3 | Switch to mock verification + email/token input; split server verification | product-owner |
| 2026-01-04 | v0.4 | Add mock state model + scenario rotation details | scrum-master |
| 2026-01-04 | v0.5 | Add UX decisions + guidance copy | ux-expert |
| 2026-01-07 | v0.6 | Implement mock verification flow, UI, and tests | dev |
| 2026-01-07 | v0.7 | Relocate BetaAccess modules and remove summary badge UI | architect |

## Dev Agent Record

### Agent Model Used

GPT-5 (Codex CLI)

### Debug Log References

- `xcodebuild test -scheme Voyager-Dev -project apps/macos/Voyager/Voyager.xcodeproj -only-testing:VoyagerTests`

### Completion Notes List

- Implemented mock verification flow with scenario rotation and Beta Access UI.
- Covered scenario rotation and gating with unit tests (UI tests skipped).
- Removed beta summary badge UI per request.
- Relocated BetaAccess modules and split step features into dedicated files.

### File List

- apps/macos/Voyager/Voyager/App/Client/BetaAccessClient.swift
- apps/macos/Voyager/Voyager/App/Features/BetaAccessFeature.swift
- apps/macos/Voyager/Voyager/App/Models/BetaAccessModels.swift
- apps/macos/Voyager/Voyager/Onboarding/Features/OnboardingFeature.swift
- apps/macos/Voyager/Voyager/Onboarding/Features/Welcome/WelcomeFeature.swift
- apps/macos/Voyager/Voyager/Onboarding/Features/Permissions/PermissionsFeature.swift
- apps/macos/Voyager/Voyager/Onboarding/Features/IndexingPreset/IndexingPresetFeature.swift
- apps/macos/Voyager/Voyager/Onboarding/Features/Complete/CompleteFeature.swift
- apps/macos/Voyager/Voyager/Onboarding/Views/OnboardingStepViews.swift
- apps/macos/Voyager/Voyager/Onboarding/Views/OnboardingView.swift
- apps/macos/Voyager/VoyagerTests/BetaAccessFeatureTests.swift
- apps/macos/Voyager/VoyagerTests/OnboardingFeatureTests.swift

## QA Results

### Review Date: 2026-01-07

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation aligns with TCA patterns; mock verification flow is deterministic and state transitions are clear. (구현이 TCA 패턴에 부합하며, 모킹 검증 흐름이 결정적이고 상태 전이가 명확합니다.)

### Refactoring Performed

None. (수정 없음.)

### Compliance Check

- Coding Standards: ✓ No violations observed in reviewed files. (검토 범위에서 위반 사항 없음.)
- Project Structure: ✓ BetaAccess is under `App/{Client,Features,Models}` and step reducers under `Onboarding/Features`. (BetaAccess는 `App/{Client,Features,Models}`, 스텝 리듀서는 `Onboarding/Features`에 위치함.)
- Testing Strategy: ✓ Unit tests cover rotation/gating; UI tests intentionally skipped per request. (로테이션/게이팅 유닛 테스트 포함, UI 테스트는 요청에 따라 생략됨.)
- All ACs Met: ✓ AC 1–6 validated via code + tests. (AC 1–6 코드/테스트로 확인됨.)

### Improvements Checklist

- [ ] Consider canceling in-flight verification when inputs change to avoid stale responses. (입력 변경 시 진행 중 검증을 취소해 응답 레이스를 줄이는 개선)
- [ ] Add a unit test for `checkFailed` → `Retry` CTA toggle. ( `checkFailed` → `Retry` CTA 전환 유닛 테스트 추가)

### Security Review

No security-sensitive operations in the mock flow. (모킹 흐름에 보안 민감 작업 없음.)

### Performance Considerations

No performance concerns for mock-only verification. (모킹 검증에 성능 이슈 없음.)

### Files Modified During Review

None. (수정 없음.)

### Gate Status

Gate: PASS → docs/qa/gates/1.3-beta-access-step-mock-verification.yml
Risk profile: docs/qa/assessments/1.3-risk-20260107.md (not created) (미작성)
NFR assessment: docs/qa/assessments/1.3-nfr-20260107.md (not created) (미작성)

### Recommended Status

[✓ Ready for Done] / [✗ Changes Required - See unchecked items above]
(Story owner decides final status)
