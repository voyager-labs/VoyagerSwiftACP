# VOY-164: 백엔드 프로젝트 및 헬퍼 런타임 점검

- Source (Linear): https://linear.app/voyager-fm/issue/VOY-164/백엔드-프로젝트-및-헬퍼-런타임-점검
- Status: local working draft (edit here, then sync back to Linear)
- Last synced: 2026-02-12

아래 내용은 Linear VOY-164 Description을 로컬로 동기화한 문서입니다.
이 파일을 기준으로 함께 수정한 뒤, 최종본을 Linear Description에 다시 반영합니다.

---

## 0) 결정사항 / 진행 방식

- 백엔드 프로젝트(`apps/backend`)는 장기적으로 **별도 서버(오케스트레이션 + 인가)** 로 분리되어 운영됨
  - 따라서 macOS 런타임/번들에 “백엔드 바이너리 포함/실행” 전제는 제거 방향
  - 백엔드 서버의 환경변수는 앱 번들 dotenv가 아니라 **서버 배포 환경(Secrets/Config)** 에서 관리
- `/api/collection` 계열에서 **LLM 통신(조건 변환) 엔드포인트/서비스는 유지**가 필요
  - 단, 현재 구현처럼 “LLM 변환 + 로컬 SQLite 검색 실행”을 서버에서 계속하는 것은 목적과 어긋남
  - 목표: `/api/collection`은 “조건 변환/오케스트레이션”로 재정의하고, 로컬 검색/SQLite/entries 도메인은 제거
- 버저닝은 배포 단위에 맞게 분리
  - macOS 앱 버전과 백엔드 서버 릴리즈 버전은 **독립적으로 관리**
  - API 계약은 `/v1` 같은 **Contract Version**으로 관리(서버 릴리즈 버전과 분리)

## 1) 배경 / 문제정의

* 현재 상태:
  * macOS 검색 실행 경로는 로컬 HTTP 백엔드(FastAPI)가 아니라 XPC 기반으로 동작
    * 호출: `apps/macos/Voyager/Voyager/04_Features/Composer/Api/SearchClient.swift` (NSXPCConnection)
    * XPC protocol/service name: `apps/macos/Voyager/Shared/Models/FilterSearchXPCProtocol.swift` (`fm.voyager.Voyager.FilterSearchXPC`)
    * XPC service entry: `apps/macos/Voyager/FilterSearchXPC/FilterSearchXPCServiceMain.swift`
    * 실제 실행: `apps/macos/Voyager/VoyagerHelper/Search/Service/FilterSearchService.swift`, `apps/macos/Voyager/VoyagerHelper/Search/Service/QuerySearchService.swift`
  * 자연어 쿼리 해석은 로컬 백엔드가 아니라 외부 게이트웨이로 요청
    * `apps/macos/Voyager/VoyagerHelper/Search/Service/QueryGatewayConverter+Gateway.swift` (env: `PUBLIC_GATEWAY_URL`)
  * 백엔드(apps/backend)는 여전히 FastAPI 런타임 + `/api/collection*` API를 유지하고 있으며, 내부에 “Helper로 이관” TODO가 존재
    * 엔드포인트: `apps/backend/src/app/search/routes.py` (`/api/collection`, `/api/collection/filters`) + `# TODO: Swift Voyager Helper로 이관 [VOY-152]`
    * 서비스/LLM 변환: `apps/backend/src/app/search/services.py`, `apps/backend/src/core/llm/search_condition_converter.py`
  * 백엔드 런타임은 `BACKEND_MODE` + `PUBLIC_BACKEND_*`에 강하게 결합되어 있음 (정리 필요)
    * dotenv 로딩: `apps/backend/src/app/config.py` (`.env.{BACKEND_MODE}` → `.env.{APP_ENV}` 순서)
    * dev/prod CLI가 `BACKEND_MODE`를 기본 세팅: `apps/backend/src/app/cli.py`
    * bundled 런타임이 `BACKEND_MODE=bundled` 기본 세팅: `apps/backend/src/app/server.py`
    * 프로세스 타이틀/host/port 의존: `apps/backend/src/app/main.py` (PUBLIC_BACKEND_PROCESS_NAME 등)
  * 그럼에도 불구하고 현재 레포에는 “로컬 FastAPI 백엔드 프로세스”를 전제로 한 실행 모드/환경키/빌드/문서가 광범위하게 남아있음
    * Helper backend lifecycle: `apps/macos/Voyager/VoyagerHelper/Infrastructure/ProcessRunner.swift`, `apps/macos/Voyager/VoyagerHelper/Infrastructure/HelperLifecycle.swift`, `apps/macos/Voyager/VoyagerHelper/Infrastructure/PortReservationService.swift`
    * BACKEND_MODE 키/스킴/Info.plist: `apps/macos/Voyager/VoyagerHelper/Info.plist`, `apps/macos/Voyager/Voyager/01_App/Config/Info.plist`, `apps/macos/Voyager/FilterSearchXPC/Info.plist`, `apps/macos/Voyager/Voyager.xcodeproj/xcshareddata/xcschemes/*.xcscheme`, `apps/macos/Voyager/Shared/EnvironmentLoader.swift`
    * `PUBLIC_BACKEND_*` 키 및 dotenv 파일(레거시 포함): `.env.source`, `.env.bundled` (삭제 대상), `.env.example`, `.env.prod`
    * 번들링 스크립트/빌드 페이즈: `scripts/build/build-backend-binary.sh`, `scripts/build/prepare-helper-runtime.sh`, `scripts/build/compile-nuitka-binary.sh`, `scripts/build/handle-backend-mode.sh`, `scripts/build/copy-bundled-env-files.sh`, `apps/macos/Voyager/Voyager.xcodeproj/project.pbxproj`
    * CI/배포 문맥: `.github/workflows/release-macos-prod.yml`, `.github/workflows/bump-version-on-release-pr.yml`, `docs/deployment.md`, `docs/troubleshooting.md`, `docs/integration/backend-bootstrap.md`, `docs/architecture/*`
  * 메인 앱도 Helper 상태 알림에서 backend 정보를 파싱/사용(ENV 갱신)하고 있음
    * HelperState 스키마에 `backend` 블록이 존재: `apps/macos/Voyager/Shared/Notifications/HelperStateNotifications.swift`
    * 메인 앱이 `backend.endpoint(host/port/url)`를 파싱하고 `Dotenv`에 `PUBLIC_BACKEND_*`를 덮어씀: `apps/macos/Voyager/Voyager/01_App/Api/HelperStateClient.swift`
    * 메인 앱이 Helper 프로세스에 `BACKEND_MODE`/`PUBLIC_BACKEND_*`를 전달: `apps/macos/Voyager/Voyager/01_App/Api/HelperAppClient.swift`
    * 사용자 메시지에도 “backend가 실행 중인지 확인” 문구가 남아있음: `apps/macos/Voyager/Voyager/02_Pages/FileManager/Reducer/FileManagerFeature.swift`
    * Helper가 브로드캐스트하는 HelperState는 `schema_version=2`이며 payload에 `backend` 블록을 항상 포함: `apps/macos/Voyager/VoyagerHelper/Infrastructure/HelperStateBroadcaster.swift`
    * 메인 앱 파서는 `schema_version`이 1/2가 아니면 무시하고, `backend` 블록이 없으면 파싱 실패: `apps/macos/Voyager/Voyager/01_App/Api/HelperStateClient.swift`
* 문제:
  * 제품 플로우에서 필요 없는 로컬 백엔드 프로세스(및 그 실행 모드)가 Helper/빌드/CI/문서 전반에 남아 있어 런타임/빌드 복잡도와 실패 지점을 키움
  * `BACKEND_MODE`, `PUBLIC_BACKEND_*`, `.env.source/.env.bundled` 같은 모드 분기/설정이 잔존하여 온보딩·디버깅·릴리즈 파이프라인 유지 비용이 지속적으로 발생
  * (전제) 백엔드 프로젝트는 장기적으로 “서버(오케스트레이션 + 인가)”로 분리되어 별도의 env로 운영될 예정
    * 그런데 현재의 `/api/collection`은 “LLM 조건 변환 + 로컬 SQLite 검색 실행”까지 한 프로세스에 결합되어 있어 서버 분리 방향과 충돌
    * 또한 env도 “앱 번들/로컬 dotenv 중심” 전제(`BACKEND_MODE`, `PUBLIC_BACKEND_*`)로 설계되어 있어 서버 env 운영 방식과 충돌
* 기대 효과:
  * Helper 런타임에서 백엔드 프로세스 라이프사이클을 완전 제거 → 부팅/재시작 루프/포트 예약/환경키 누락 등 클래스의 문제 제거
  * 빌드/릴리즈 파이프라인에서 backend 번들링 단계 제거 → 속도/안정성 개선
  * 백엔드 쪽도 “서버 분리”에 맞게 실행 모드/환경키/로컬 검색 도메인 의존을 정리하여 SSOT 단순화

## 2) 관련 스펙 링크

* IA: `03_INFORMATION_ARCHITECTURE/index.md` - N/A (repo 내 파일 없음)
* Feature Inventory: `04_FEATURE_INVENTORY/index.md` - N/A (repo 내 파일 없음)
* Use Cases: `05_USE_CASES/index.md` - N/A (repo 내 파일 없음)
* 기타 참고: `docs/macos/voyager-helper.md`, `docs/integration/backend-bootstrap.md`, `docs/features/search.md`, `docs/architecture/overview.md`, `docs/architecture/environment.md`, `docs/troubleshooting.md`, 관련 이슈: [VOY-118](https://linear.app/voyager-fm/issue/VOY-118)

## 3) 구현 범위

### In Scope

- [x] Helper 내 “백엔드 프로세스 라이프사이클” 로직을 가드레일 없이 완전 삭제
  * 대상: `apps/macos/Voyager/VoyagerHelper/Infrastructure/ProcessRunner.swift`, `apps/macos/Voyager/VoyagerHelper/Infrastructure/HelperLifecycle.swift`, `apps/macos/Voyager/VoyagerHelper/Infrastructure/PortReservationService.swift`, 및 연관 코드(backend start/monitor/restart/ready-check)
- [x] `BACKEND_MODE` 환경키/값/주입 경로를 전부 제거 (macOS + 로컬 번들링 경로)
  * 대상(macOS): Xcode schemes(`apps/macos/Voyager/Voyager.xcodeproj/xcshareddata/xcschemes/*.xcscheme`), Info.plist(`apps/macos/Voyager/VoyagerHelper/Info.plist`, `apps/macos/Voyager/Voyager/01_App/Config/Info.plist`, `apps/macos/Voyager/FilterSearchXPC/Info.plist`), 로더 코드(`apps/macos/Voyager/Shared/EnvironmentLoader.swift`)
  * 대상(macOS): build settings에도 `BACKEND_MODE=source/bundled`가 박혀 있음 → 제거 필요: `apps/macos/Voyager/Voyager.xcodeproj/project.pbxproj`
  * 대상(Helper): 실행 로그에서 `Dotenv.backendMode`를 출력하는 구문 제거/대체: `apps/macos/Voyager/VoyagerHelper/VoyagerHelperApp.swift`
  * 대체 로딩 정책: `BACKEND_MODE` 없이 `.env.{APP_ENV}`를 로드하도록 변경 (project root 우선 → bundle resources fallback)
    * 참고: `EnvironmentLoader.BackendMode.inferProjectRoot`는 현재 `apps/backend` 존재로도 root를 추론하므로, 서버 분리 후에는 `apps/macos/Voyager`(또는 `Voyager.xcodeproj`) 기준으로만 추론하도록 정리 필요
  * 백엔드 서버는 별도 env로 운영되므로, `BACKEND_MODE` 같은 “source/bundled” 구분 자체가 서버 컨텍스트에선 무의미해짐 → 서버 코드에서도 제거/대체 필요(단, 이 이슈에서는 스코핑/가이드까지)
- [x] `PUBLIC_BACKEND_HOST`, `PUBLIC_BACKEND_PORT`, `PUBLIC_BACKEND_PROCESS_NAME`, `PUBLIC_BACKEND_URL` 환경키를 전부 제거 (macOS/번들링/레거시 경로)
  * 대상: Swift 코드(Helper/App), 문서, `.env.*` 파일, build scripts, 레거시 가이드
  * 서버 분리 이후의 백엔드 주소/포트/프로세스 타이틀은 “PUBLIC_*”로 노출할 성질이 아니라 서버 런타임 설정으로 관리
  * 제거/정리 포인트: `apps/macos/Voyager/Shared/EnvironmentLoader.swift`의 `Dotenv.publicBackendURL` (fatalError 기반)
- [x] `.env.source`, `.env.bundled` 파일 삭제 및 빌드/런타임에서 해당 파일명 참조 제거 (macOS 기준)
  * 대상: env 로더/빌드 스크립트/Xcode Build Phase/문서 내 언급
  * `.env.prod`는 게이트웨이/웹/로깅/Sentry 등 제품 설정으로 유지
- [x] backend 번들링/빌드 관련 스크립트 및 Xcode Build Phase 제거
  * 대상: `scripts/build/build-backend-binary.sh`, `scripts/build/prepare-helper-runtime.sh`, `scripts/build/compile-nuitka-binary.sh`, `scripts/build/handle-backend-mode.sh`, `scripts/build/copy-bundled-env-files.sh`, `apps/macos/Voyager/Voyager.xcodeproj/project.pbxproj`
- [x] CI/릴리즈 파이프라인에서 backend 번들링/uv 의존 제거(해당되는 부분)
  * 대상: `.github/workflows/release-macos-prod.yml` 및 관련 스크립트
- [x] Helper 상태 브로드캐스트/앱 파서에서 backend 블록/endpoint 업데이트 제거
  * 대상: `apps/macos/Voyager/VoyagerHelper/Infrastructure/HelperStateBroadcaster.swift`, `apps/macos/Voyager/Voyager/01_App/Api/HelperStateClient.swift`, `apps/macos/Voyager/Shared/Notifications/HelperStateNotifications.swift`
  * 목표: 메인 앱이 Helper에서 backend endpoint를 받아 `PUBLIC_BACKEND_*`를 갱신하는 흐름 자체를 삭제
  * 세부:
    * HelperState payload에서 `backend` key 제거 + `schema_version` bump (예: 3)
    * 메인 앱 파서는 `schema_version=3`을 인식하고 `backend` 없이도 HelperState를 구성(또는 backend를 optional로)
    * 전환기 호환(=v2 수용)은 하지 않음: v3 only로 즉시 전환

- [x] 메인 앱 ↔ Helper 사이의 “backend ENV 전달” 제거
  * 대상: `apps/macos/Voyager/Voyager/01_App/Api/HelperAppClient.swift`의 환경키 리스트에서 `BACKEND_MODE`, `PUBLIC_BACKEND_*`, `VOYAGER_DISABLE_BACKEND` 제거
  * Helper가 `.env`/프로세스 env에서 `PUBLIC_BACKEND_*`를 요구하는 경로가 있다면 함께 제거(Helper backend lifecycle 삭제와 세트)
  * 목표: Helper 부팅/검색(XPC + Gateway)에는 backend ENV가 전혀 필요하지 않게 만들기

- [x] 사용자-facing 문구/에러 메시지에서 backend 의존 표현 제거
  * 대상: `apps/macos/Voyager/Voyager/02_Pages/FileManager/Reducer/FileManagerFeature.swift` 등
  * 목표: 실패 원인을 “backend 실행”으로 오인시키지 않기
- [x] 백엔드(apps/backend) 정리 항목 스코핑 (서버 분리 대비)
  * 목표: “서버(오케스트레이션 + 인가)”로 전환 시, **유지할 것(LLM 통신)** 과 **제거할 것(로컬 검색/SQLite)** 를 분리
  * 유지(남겨야 함): LLM 조건 변환/오케스트레이션 핵심
    * `apps/backend/src/core/llm/langchain_provider.py` (게이트웨이 기반 LLM provider)
    * `apps/backend/src/core/llm/search_condition_converter.py`
    * `apps/backend/src/core/llm/prompts/compose_filter_system.md`
    * `apps/backend/src/core/metadata/registry_loader.py` (+ `shared/*.json`)
  * 제거(서버 분리 방향에서 불필요/리스크 큼): 로컬 파일검색 결합부
    * 로컬 SQLite/Entry 도메인: `apps/backend/src/infra/db/*`, `apps/backend/src/infra/schemas/entry_schema.py`, `apps/backend/src/infra/repositories/*`
    * 조건 빌더/SQL 실행: `apps/backend/src/core/search/*`
    * `/api/collection`의 “검색 실행” 기능(LLM 변환 이후 execute_search): `apps/backend/src/app/search/services.py`
  * API 정리 방향:
    * `/api/collection`는 “LLM 통신(조건 변환)” 엔드포인트로 남기되, **items 조회/DB 실행은 하지 않도록** 재정의
    * 현재 `SearchResponse` 스키마를 당장 유지해야 한다면, 변환 엔드포인트에서는 `items=[]`, `itemCount=0`을 반환하고 `appliedFilters`만 의미 있게 채우는 형태로 점진 전환
    * 이후 계약 버전(`/v1`)을 도입해 “convert-only response schema”로 정리

- [ ] 버저닝/배포 단위 분리 정책을 명시 (문서/워크플로우)
  * backend 서버 릴리즈 버전(이미지 태그/배포 버전)과 macOS 앱 버전은 독립
  * API 계약 버전(`/v1`)은 별도로 관리
  * CI에서 backend 버전 bump가 macOS 릴리즈와 결합되어 있다면 분리 고려: `.github/workflows/bump-version-on-release-pr.yml`

### Out of Scope

* 게이트웨이 API 계약/모델 변경(VOY-118에서 다룸)
* 검색 알고리즘/DB 구조 대규모 변경(GRDB/XPC 검색 로직 자체 리팩터링)
* `apps/backend` 디렉터리 전체 삭제/아카이브(필요 시 별도 이슈로 분리)

## 4) 상세 요구사항

- [x] 기능 요구사항 A: Helper는 백엔드 프로세스를 spawn/monitor/restart 하지 않고도 정상 부팅 및 검색(XPC)을 제공
- [x] 기능 요구사항 B: 빌드/릴리즈에서 backend 번들링 단계가 제거되어 추가 툴링(uv/Nuitka) 없이도 macOS 빌드가 가능
- [x] 기능 요구사항 C: 빌드 산출물/앱 번들 리소스에 `.env.source`/`.env.bundled`가 포함되지 않음 (Release 기준 `.env.prod`만 포함)
- [x] 기능 요구사항 D: (서버 분리) `/api/collection`은 LLM 조건 변환을 제공하며, 로컬 SQLite/entries 검색 실행에 의존하지 않음
- [ ] 권한/가드 조건:
  * macOS 런타임에서 backend 관련 환경키(`BACKEND_MODE`, `PUBLIC_BACKEND_*`)가 제거되어도 크래시(fatalError) 없이 동작
  * (서버 분리) 백엔드 API는 인증/인가를 전제로 하며, 인증 실패 시 401/403을 일관된 스키마로 반환
- [ ] 에러/예외 처리 기준:
  * 게이트웨이 실패/LLM 변환 실패는 일관된 에러 코드로 반환(예: `LLM_CONVERSION_FAILED`)
  * macOS 검색 실패와 backend 변환 실패는 로그/에러 코드로 구분 가능해야 함

## 5) 상태/시나리오 정의

* 정상:
  * Helper 부팅 → DB init/migration → helper_ready 브로드캐스트 → XPC 검색 요청 처리
* 로딩:
  * 초기 인덱싱/DB 초기화 중에도 앱은 helper_ready 상태를 정상 수신/표시
* 빈 상태:
  * 인덱싱 전/검색 결과 없음 → itemCount=0
* 오류:
  * 게이트웨이 오류/LLM 변환 실패 → `SearchResponsePayload.error`로 처리
* 엣지 케이스:
  * `.env`에 backend 관련 키가 전혀 없어도 Helper/App이 정상 부팅
  * Dev/Prod 스킴 간 “BACKEND_MODE 차이”가 사라져도(키 자체 제거) 동일한 런타임 동작을 보장

## 6) 데이터/계약 변경 (해당 시)

* API:
  * 제품 경로에서 “로컬 검색 실행” 용도의 `/api/collection*`는 더 이상 사용하지 않음(문서/레거시 가이드 제거)
  * 대신 `/api/collection`은 “LLM 조건 변환/오케스트레이션”로 의미를 재정의(Contract Version 도입 전까지는 점진 전환)
* DB/Storage:
  * 변경 없음(Helper DB/인덱싱 유지)
* 이벤트/로깅:
  * HelperState payload에서 backend 블록 제거(필요 시 schema_version 업데이트)
  * backend lifecycle 관련 로그 제거

## 7) Acceptance Criteria (Given / When / Then)

1. **Given** 최신 빌드/런타임, **When** Voyager 앱과 Helper를 실행한다, **Then** 로컬 백엔드 프로세스가 실행되지 않고도 검색(XPC + Gateway)이 정상 동작한다.
2. **Given** 환경에 `BACKEND_MODE` 및 `PUBLIC_BACKEND_*`가 존재하지 않는다, **When** Voyager 앱과 Helper를 실행한다, **Then** fatalError/재시작 루프 없이 정상 부팅 및 상태 브로드캐스트가 동작한다.
3. **Given** 릴리즈 빌드/CI, **When** macOS release workflow를 실행한다, **Then** backend 번들링/uv/Nuitka 단계 없이도 빌드가 성공한다.
4. **Given** Release 빌드 산출물, **When** 앱/Helper 번들 리소스를 확인한다, **Then** `.env.source`/`.env.bundled`는 존재하지 않고 `.env.prod`만 포함된다.
5. **Given** (서버 분리) 유효한 인증 토큰과 자연어 query, **When** 클라이언트가 backend의 `/api/collection`에 요청한다, **Then** 서버는 LLM 조건 변환을 수행하고 `appliedFilters`(conditions/scopes)를 반환하며 로컬 SQLite/entries 조회는 수행하지 않는다.
6. **Given** (서버 분리) 인증 토큰이 없거나 유효하지 않다, **When** `/api/collection`에 요청한다, **Then** 401(또는 403)을 일관된 포맷으로 반환한다.
7. **Given** Helper가 상태 알림을 브로드캐스트한다, **When** 메인 앱이 HelperState를 파싱한다, **Then** `backend` 블록/endpoint를 요구하지 않고 `PUBLIC_BACKEND_*`를 설정하지 않는다.
8. **Given** Collection 검색/필터 적용이 실패한다, **When** 앱이 사용자에게 오류를 표시한다, **Then** “backend가 실행 중인지 확인” 같은 오해 소지가 있는 문구가 아니라 Gateway/Helper/DB 등 실제 실패 원인에 맞는 문구를 안내한다.
