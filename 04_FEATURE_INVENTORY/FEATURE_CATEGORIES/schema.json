{
  "schema_version": "1.0",
  "name": "FEATURE_CATEGORIES",
  "primary_key": ["category_key"],
  "columns": [
    {
      "name": "order",
      "type": "number",
      "required": false,
      "description": "표시 순서(낮을수록 먼저 표시)."
    },
    {
      "name": "category_title",
      "type": "string",
      "required": true,
      "description": "카테고리 제목(사람이 읽는 라벨)."
    },
    {
      "name": "category_key",
      "type": "string",
      "required": true,
      "description": "카테고리 키(PK). 안정적인 참조 키."
    },
    {
      "name": "description",
      "type": "string",
      "required": false,
      "description": "카테고리 설명."
    }
  ]
}
